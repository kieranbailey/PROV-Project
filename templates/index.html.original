<!DOCTYPE html>
<html>
<head>
    <title>Image Downloader</title>
</head>
<body>
    <h1>Available Images</h1>
    <form action="/download" method="post">
        <ul>
            {% for item in image_items %}
            <li>
                <label>
                    <input type="checkbox" name="selected_images" value="{{ item['_id'] }}">
                    <img src="{{ item['iiif-thumbnail'] }}" alt="Thumbnail">
                </label>
            </li>
            {% endfor %}
        </ul>
        <button type="submit">Download Selected Images</button>
    </form>
</body>
</html>
